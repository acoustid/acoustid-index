syntax = "proto3";

package fpindex;

enum SegmentBlockFormat {
  BLOCK_FORMAT_UNSPECIFIED = 0;
  BLOCK_FORMAT_V1 = 1;
}

message SegmentHeader {
  uint32 id = 1;
  uint32 block_size = 2;
  SegmentBlockFormat block_format = 3;
}
