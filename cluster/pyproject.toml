[project]
name = "fpindex-cluster"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fpindex-cluster = "fpindexcluster.__main__:main"

[tool.uv]
package = true
